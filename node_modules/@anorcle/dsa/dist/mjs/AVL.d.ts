import { BST } from './index.js';
import type { Node } from './BST.js';
import type { compare } from './types/index.js';
declare class AVL<T> extends BST<T> {
    constructor(compare: compare<T>);
    private rotateRight;
    private rotateLeft;
    private rebalanceRight;
    private rebalanceLeft;
    private rebalance;
    insert(data: T): Node<T>;
    delete(data: T): void;
}
export default AVL;
