import { AVL } from './index.js';
class Set extends AVL {
    constructor(compare) {
        super(compare);
    }
    insert(data) {
        const node = this.findNode(data);
        if (node) {
            node.data = data;
            return node;
        }
        return super.insert(data);
    }
    find(data) {
        const node = super.findNode(data);
        return node ? node.data : null;
    }
}
export default Set;
